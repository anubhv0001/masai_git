/*Write a function doubleNumbers() that takes an array of numbers and returns a new array where each number is doubled.

Requirements:
Use .map() to iterate over the array and double each number.
Example:
]*/
  const numbers = [1, 2, 3, 4].map((ele)=>ele*2);
  console.log(numbers);
  