constructor studentsManager(name,score,subject){
    this.name=name,
    this.score=score,
    this.subject=subject
  }
  const studentDetails={};
  const addStudents(){
    studentDetails.push(name);
  }
  const score(){
    studentDetails.push(socre);
  }
  const addSubject(){
    studentDetails.push(subject);
  }
  
  studentDetails.addStudents(Anubhav);